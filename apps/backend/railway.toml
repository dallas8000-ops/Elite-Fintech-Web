# Railway service: elite-fintech-systems-api
# Set service root directory to apps/backend in the Railway dashboard.

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
